interface:
  display_name: "Encoding Guard"
  short_description: "Minimal encoding policy helper for Chinese output."
  default_prompt: "Before outputting Chinese text, resolve encoding via get-output-encoding. If policy is missing, create minimal .encoding-policy.json."
